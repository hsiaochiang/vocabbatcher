# 會考單字練習 App — 使用手冊

> 這份手冊教你怎麼使用「會考單字練習」App。
> 不需要任何電腦知識，只要會用手機瀏覽器就可以開始。

---

## 目錄

1. [第一次開啟 App](#一第一次開啟-app)
2. [完整學習一輪](#二完整學習一輪從選字到翻牌)
3. [如何篩選單字](#三如何篩選單字)
4. [如何選字並建立批次](#四如何選字並建立批次)
5. [如何學習翻牌卡](#五如何學習翻牌卡)
6. [如何聽單字發音](#六如何聽單字發音)
7. [如何繼續上次未完成的批次](#七如何繼續上次未完成的批次)
8. [如何查看與管理歷史批次](#八如何查看與管理歷史批次)
9. [常見問題](#九常見問題)

---

## 一、第一次開啟 App

### 用手機瀏覽器打開

1. 打開手機上的 **Chrome**（Android）或 **Safari**（iPhone）。
2. 在網址列輸入老師給你的網址，按「前往」。
3. 等幾秒鐘，你會看到一個藍色標題寫著「**國中會考單字準備**」的畫面。這就是 App 的首頁。

> 如果你看到的是空白畫面或錯誤訊息，請確認網址有沒有打對，然後重新整理一次（往下拉再放開）。

### 把 App 加到手機桌面

加到桌面後，以後只要點桌面上的圖示就能打開，就像普通的 App 一樣。

**Android 手機（Chrome）：**
1. 打開 App 後，畫面上方可能會自動跳出一個「加到主畫面」的提示。如果有，直接點「安裝」就好。
2. 如果沒有自動跳出，點畫面右上角的「⋮」（三個小點），找到「加到主畫面」或「安裝應用程式」，點一下。
3. 按「新增」或「安裝」確認。
4. 回到手機桌面，你會看到一個寫著「會考單字」的圖示。點它就能全螢幕開啟 App（沒有網址列）。

**iPhone（Safari）：**
1. 打開 App 後，點畫面最下方中間的「分享」按鈕（一個方框加一個向上箭頭的圖示）。
2. 往下滑，找到「加入主畫面」，點一下。
3. 畫面會出現一個確認框，直接點右上角的「新增」。
4. 回到手機桌面，你會看到 App 圖示。點它就能全螢幕開啟。

> 如果你在 iPhone 上找不到「加入主畫面」的選項，請確認你使用的是 Safari 瀏覽器，不是 Chrome。iPhone 的 Chrome 不支援這個功能。

---

## 二、完整學習一輪（從選字到翻牌）

這一節帶你從頭到尾完成一次學習流程。如果你是第一次用，建議先照著做一遍。

### 步驟 1：建立一個新批次

1. 在首頁（有「國中會考單字準備」標題的畫面），找到最下方的藍色按鈕，上面寫著「**＋ 建立新批次**」。點一下。
2. 你會看到一個新的畫面，標題是「**批次建立器**」。畫面上列出了 1231 個英文單字。

### 步驟 2：勾選想學的單字

1. 用手指往下滑，瀏覽單字清單。每個單字都會顯示英文、詞性（像是「名詞」「動詞」），以及中文意思。
2. 點一下你想學的單字，它左邊會出現一個**藍色打勾**的框框，表示已經選了這個字。
3. 試著選 5 個字就好。畫面上方的計數器會顯示「**已選 5 / 25**」。

### 步驟 3：建立批次

1. 選完後，找到畫面最下方的藍色按鈕，上面寫著「**建立批次（5 字）**」。點一下。
2. App 會自動跳到一個新畫面，標題是「**批次 #1**」。這就是你剛建立的批次。

### 步驟 4：開始翻牌學習

1. 在這個畫面上，你會看到四張卡片。找到左上角那張有藍色底色的「**翻牌學習**」卡片，點一下。
2. 你會看到翻牌卡畫面，中間有一張大卡片，上面顯示一個**英文單字**和**音標**（如果有的話）。

### 步驟 5：學習第一張牌

1. 看一下卡片上的英文單字。
2. 卡片中間有一個寫著「**播放發音**」的按鈕，旁邊有個喇叭圖示。點一下，你會聽到這個字的英文發音。
3. 準備好了以後，**點卡片的任何位置**（或是點畫面下方的「翻面」按鈕）。
4. 卡片會翻轉過來，你會看到這個字的**中文意思**和**詞性**。同時，App 會自動唸一次中文。

### 步驟 6：翻下一張

1. 看完這張牌以後，點畫面右下方的「**下一張 →**」按鈕。
2. 卡片會翻回正面，顯示下一個英文單字。
3. 重複「看英文 → 聽發音 → 翻面 → 看中文」這個流程。

### 步驟 7：翻完全部

1. 當你翻完最後一張，畫面會出現一個慶祝圖示，寫著「**這輪結束！**」和「你已經看完全部 5 張翻牌」。
2. 如果想再練一次，點「**重新開始**」按鈕。
3. 如果不想繼續，點畫面左上角的「**← 返回**」箭頭，回到批次 Hub。

### 步驟 8：確認進度已儲存

1. 點左上角的「**← 返回**」箭頭回到首頁。
2. 你會在首頁看到「**歷史批次**」區域列出了你剛才建立的「批次 #1」。
3. 這個批次的進度是自動儲存的。就算你關掉 App 再打開，它還是在的。

---

## 三、如何篩選單字

> 這一節幫你從 1231 個單字中，用篩選條件快速找到你想學的字。

### 進入篩選畫面

1. 在首頁點最下方的「**＋ 建立新批次**」按鈕，進入「批次建立器」。
2. 畫面最上方有兩排小按鈕（篩選條件），下面有一個搜尋框。

### 按出現頻率篩選

在第一排按鈕中：
- 點「**全部**」→ 顯示全部 1231 個字
- 點「**高頻 ≥8**」→ 只顯示在歷屆考題中出現 8 次以上的字（最常考的）
- 點「**中頻 4-7**」→ 只顯示出現 4 到 7 次的字
- 點「**低頻 ≤3**」→ 只顯示出現 3 次以下的字（比較少考的）

> 建議：如果不確定要學什麼，先從「高頻 ≥8」開始，這些是會考最常出現的字。

### 按詞性篩選

在第二排按鈕中：
- 點「**全部詞性**」→ 不限制詞性
- 點「**名詞**」→ 只顯示名詞（例如 apple、book）
- 點「**動詞**」→ 只顯示動詞（例如 run、eat）
- 點「**形容詞**」→ 只顯示形容詞（例如 beautiful、happy）
- 點「**副詞**」→ 只顯示副詞（例如 always、quickly）
- 點「**代名詞**」→ 只顯示代名詞（例如 he、she、they）
- 點「**介系詞**」→ 只顯示介系詞（例如 in、on、at）
- 點「**連接詞**」→ 只顯示連接詞（例如 and、but）
- 點「**其他**」→ 顯示其他類型（助動詞、冠詞等）

### 用搜尋框找特定單字

1. 在篩選按鈕下方，有一個輸入框，裡面寫著「搜尋英文單字…」。
2. 點一下那個框，用鍵盤輸入你要找的字。例如輸入 **app**。
3. 清單會即時篩選，只剩下包含 "app" 的字，像是 apple、appear、happy。
4. 想清除搜尋：把輸入框裡的文字全部刪掉就好。

### 同時使用多個篩選

你可以同時選頻率、詞性和搜尋。例如：
- 先點「高頻 ≥8」
- 再點「名詞」
- 這樣只會顯示「高頻的名詞」

### 清除篩選

想回到全部 1231 個字？
- 把頻率改回「**全部**」
- 把詞性改回「**全部詞性**」
- 把搜尋框清空

---

## 四、如何選字並建立批次

> 這一節幫你學會怎麼勾選單字、管理勾選、以及建立批次。

### 勾選單字

1. 在批次建立器中，點一下你想學的單字。它左邊會出現一個**藍色打勾的框框**。
2. 再點一下同一個字，勾選就會取消。

### 看目前選了幾個

畫面上方（篩選條件的下面）有一行小字，寫著「**已選 N / 25**」。每次你勾選或取消，這個數字會馬上更新。

### 選滿 25 個以後

每個批次最多只能放 25 個字。這是為了避免一次學太多、效果變差。

- 當你選滿 25 個，計數器會變成藍色的「**已選 25 / 25**」，旁邊出現一個提示圖示。
- 剩下沒選的字會**變成灰色**，而且點不了。
- 如果你還是點了灰色的字，畫面下方會短暫出現一行字：「**最多只能選 25 個單字**」。

### 快速選字

- 想一次清空所有勾選？點計數器旁邊紅色的「**清除**」按鈕。
- 想快速選滿？點「**全選篩選結果**」按鈕，App 會自動從目前顯示的字裡面選，直到 25 個為止。

> 小技巧：先用篩選縮小範圍（例如只看「高頻名詞」），再按「全選篩選結果」，就能快速建立一組高頻名詞批次。

### 建立批次

1. 選完字以後，看畫面最下方。有一個藍色大按鈕，寫著「**建立批次（N 字）**」，N 是你選的字數。
2. 點一下那個按鈕。
3. App 會自動幫你建立批次（名稱是「批次 #1」、「批次 #2」⋯⋯依序編號），然後跳到批次 Hub 頁面。

> 如果那個按鈕是灰色的、按不了，表示你還沒有勾選任何字。至少要選 1 個字才能建立。

---

## 五、如何學習翻牌卡

> 這一節幫你學會怎麼用翻牌卡來記單字。

### 進入翻牌學習

1. 從首頁點一個批次（或點「繼續」），進入批次 Hub。
2. 在批次 Hub 畫面上，找到左上角那張有藍色底色的「**翻牌學習**」卡片，點一下。

### 翻牌卡的正面

翻牌卡的正面顯示：
- **英文單字**（大字，畫面正中央）
- **音標**（如果這個字有音標的話，會顯示在英文下方，例如 `/ˈæp.əl/`）
- **「播放發音」按鈕**（喇叭圖示）
- 最下方有一行小字「**點擊翻面**」

### 翻牌卡的背面

翻牌卡的背面顯示：
- **英文單字**（小字，在上方）
- **中文定義**（大字，在中間，例如「蘋果」）
- **詞性標籤**（例如「n.」表示名詞）
- **「播放中文」按鈕**（喇叭圖示）

### 怎麼翻面

你有兩種方式：
- **直接點卡片**的任何位置
- 或是點畫面下方中間的「**翻面**」按鈕

卡片會有一個翻轉的動畫效果。

### 翻到下一張 / 上一張

畫面最下方有三個按鈕：
- **← 上一張**：回到前一張牌（如果已經是第一張，這個按鈕會是灰色的，按不了）
- **翻面**：翻轉目前這張卡片
- **下一張 →**：前進到下一張牌

### 翻完一輪

當你翻完最後一張卡片，再按「下一張」，畫面會出現：
- 一個慶祝圖示 🎉
- 「**這輪結束！**」
- 「你已經看完全部 N 張翻牌」
- 右下方的按鈕會變成「**重新開始**」，點了就從第一張重新開始

### 看目前進度

畫面上方有一條進度列，顯示：
- 「已看 N / M 張」（N 是目前看到第幾張，M 是總共幾張）
- 旁邊有百分比（例如「60%」）
- 下方有一條藍色的進度條

---

## 六、如何聽單字發音

> 這一節幫你學會怎麼用 App 聽英文和中文的發音。

### 聽英文發音

在翻牌卡的正面（顯示英文的那面），找到寫著「**播放發音**」的按鈕，旁邊有一個喇叭圖示。點一下，你會聽到這個字的英文發音。

### 聽中文發音

- **自動播放：** 當你翻到背面（中文面）的時候，App 會**自動播放一次**中文發音。
- **手動再聽一次：** 背面有一個寫著「**播放中文**」的按鈕，點一下就能再聽一次。

### 某些字沒有音標

有些字下面不會顯示音標，這是正常的。這些字大多是很基本的字（像是 and、at、be），在字典裡沒有提供國際音標。不影響使用——你還是可以點「播放發音」按鈕來聽發音。

### 按了沒聲音？

- 請確認手機的**音量有開啟**，不要在靜音模式。
- **iPhone 使用者：** 第一次使用時，你需要先點一次畫面上的任何按鈕，之後聲音才會正常播放。這是 iPhone 瀏覽器的安全限制。
- **Android Chrome：** 大部分情況下都可以直接播放。

---

## 七、如何繼續上次未完成的批次

> 這一節幫你學會怎麼接續之前學到一半的進度。

### 從首頁繼續

1. 打開 App，你會看到首頁。
2. 如果你上次有使用某個批次，首頁最上方會有一個區塊，標題是「**繼續上次**」。
3. 這個區塊會顯示批次的名稱、你上次翻到第幾張（例如「已學 3 / 5 張」），以及一條進度條。
4. 點「**繼續**」按鈕，就會直接跳到批次 Hub。再點「翻牌學習」，你會從上次停下的地方繼續。

### 進度是自動儲存的

你不需要做任何「存檔」的動作。每翻一張牌，App 就會自動記住你翻到第幾張。就算你：
- 關掉 App 再打開
- 重新整理頁面
- 手機突然沒電

只要不是清除瀏覽器資料，你的進度都不會消失。

---

## 八、如何查看與管理歷史批次

> 這一節幫你學會怎麼查看過去建立的批次，以及怎麼刪除不要的批次。

### 查看歷史批次

1. 打開 App 首頁。
2. 往下看，你會看到「**歷史批次**」區域。
3. 每個批次會顯示：
   - 批次名稱（例如「批次 #1」）
   - 建立日期
   - 包含幾個字
4. 點任何一個批次，就會進入那個批次的 Hub 頁面。

### 重新學習一個舊批次

1. 在首頁點那個批次。
2. 進入批次 Hub 後，點「**翻牌學習**」。
3. 如果上次已經翻完全部，點「重新開始」就能從頭再學一次。

### 刪除批次

1. 在首頁點你想刪除的批次，進入批次 Hub。
2. 在畫面右上角，有一個**垃圾桶圖示**。點一下。
3. 畫面會跳出一個確認框：「確定刪除此批次？」
4. 點「**確定**」就會刪除。點「取消」就不刪。

> 注意：刪除後就沒辦法恢復了。如果不確定，先不要刪。

---

## 九、常見問題

### Q1：每個批次最多能放幾個字？

每個批次上限是 **25 個字**。這是根據學習研究建議的：一次學太多字反而記不住，25 個左右是比較有效的數量。你可以建立很多個批次，所以不用擔心不夠用。

### Q2：有些字沒有中文定義，是 App 壞掉了嗎？

不是壞掉。原始的單字資料中，有大約 37 個字本來就沒有中文定義（例如 he、I、not 這類太基本的字）。這些字在翻牌卡背面會顯示「（無中文定義）」。它們的英文發音仍然可以正常使用。

### Q3：沒有網路的時候可以用嗎？

可以。第一次用瀏覽器打開 App 時，它會自動把所有資料下載到你的手機。之後就算沒有網路也可以正常使用——包括看單字、翻牌、聽發音（發音使用手機內建的功能，不需要網路）。

唯一的例外是：**第一次打開必須有網路**，因為要先把 App 下載下來。

### Q4：我關掉 App 再打開，資料還在嗎？

在。你的批次資料存在手機的瀏覽器裡面，關掉再打開不會消失。但是有一種情況會讓資料消失：**清除瀏覽器資料**。如果你在手機設定裡清除了 Chrome 或 Safari 的資料，批次和進度都會被刪掉。

建議：不要清除「會考單字」這個網站的瀏覽器資料。

### Q5：App 可以在電腦上用嗎？

可以。用電腦的 Chrome 或 Edge 瀏覽器打開同一個網址就可以。電腦上的語音品質通常比手機更好。不過電腦版沒有「加到桌面」的功能，你可以把網址加到書籤裡方便下次使用。

---

## 附錄

### 關於單字庫

這個 App 使用的單字庫來自「115 學年度國中教育會考常用 2000 字」（top2025），共收錄 **1231 個單字**。每個單字都標示了：

- 在歷屆考題中的出現頻率（1–10 次）
- 詞性（名詞、動詞、形容詞⋯⋯）
- 中文定義
- 國際音標（約 94% 的字有提供）
- 原始 PDF 頁碼

### 版本資訊

- App 版本：M2（核心功能版）
- 單字庫版本：top2025 v1
- 最後更新：2026 年 5 月 31 日
