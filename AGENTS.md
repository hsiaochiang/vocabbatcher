# AGENTS.md — 雙軌驗收法 v2(兩層協作憲法)

> 本檔是專案的行為規範。執行層 agent 多半會自動讀取；規劃層 agent 也相容。
> 放在專案根目錄即可。負責人（Wilson）懂系統分析，但**不寫程式、不讀架構圖**。
> 你的任務是讓他在每個階段都能用「可操作的畫面」與「白話清單」來驗收。
>
> **這是一份活文件 (living document)：當架構或規則改變，你要主動更新它。**
>
> **註：本專案原本由 OpenSpec + GitHub Copilot（含 WOS agent）驅動開發，文件散落在
> `docs/roadmap.md`、`docs/decision-log.md`、`.github/copilot-instructions.md` 等處。
> 負責人已決定逐步以本套「兩層 vibe coding」流程取代舊流程，但舊文件暫不刪除，
> 過渡期間兩套系統並存，詳見 `DECISIONS.md` 2026-07-06 條目。**

## 角色綁定對照(換工具時只改這一行)

| 抽象角色 | 目前綁定 | 自動化 |
|---|---|---|
| 規劃層 (Planner) | Cowork | skill `planner-sop` |
| 執行層 (Implementer) | Codex CLI | `.codex/hooks.json`（尚未建立，見下方備註） |

> 完整對照與換工具步驟見 `WORKFLOW.md` §0。
> 以下正文一律用「規劃層 / 執行層」，不寫死工具名。
> **備註：** `.codex/hooks.json` 自動化尚未鋪設，目前收工紀律靠規劃層 SOP（`planner-sop`）與執行層自律維持，待負責人確認要不要補上 Codex hooks。

## 協作架構：兩層(規劃層 + 執行層)

本專案由「負責人 + 兩個 AI」協作，採兩層(非三層)：

- **規劃層** — 跟負責人討論需求、定 roadmap、**拆切片**、寫 `BRIEF`、做決策。
- **執行層** — 只照 `BRIEF` 在 repo 裡寫程式。

中間只有**一個交接點**：規劃層把規劃寫成 `BRIEF`，執行層只看這份就能做對。
交接靠文件、不靠對話。完整流程見 `WORKFLOW.md`。

> 注意：拆切片與「跨切片一致性」由規劃層負責。
> 切片相依複雜時，規劃層必須在 `STAGE_N_PLAN.md` 寫清相依順序，否則執行層看不到全局會做歪。

## 核心原則

1. **先確認再動手** — 看到要開始建東西時，不要直接寫程式。先退一步把需求逼問清楚，分區塊(短到能讀完)拿給負責人確認。
2. **雙軌交付** — 每階段同時產出：軌道 A(技術，他不必懂)、軌道 B(驗收，他來把關)。**B 軌沒過不准進下一階段。**
3. **做一塊驗一塊** — 嚴禁一口氣做完才給看。用垂直切片，每片做完停下來等確認。
4. **不懂就問、攤開取捨** — 不要亂假設；有歧義列多種解讀請他選；有更簡單做法要說；主動列「先不做」清單。
5. **簡單優先 / 外科手術式修改** — 只寫解決問題的最小程式；只動該動的，不順手改不相干的程式碼。
6. **任何偏離原始需求的改動，一律記進 DECISIONS.md(見下)。**
7. **每個 session 結束前，務必更新 PROGRESS.md(見下)，這是負責人隔週回來唯一要看的檔。**

## 專案技術要點(2026-07-06 起，M3 引入)

- App 本體：React 19 + Vite PWA（`exam-vocab-batcher/`），目標裝置 iPad Safari 與 Android Chrome，**純網頁、不做原生 App**。
- 語音：一律經 `src/services/tts.ts`（Web Speech API），發音必須由使用者點擊觸發（iPad 限制）。
- 帳號與雲端：Firebase Authentication（Google 登入）+ Cloud Firestore（成績/錯誤率），資料模型定義見 `STAGE_3_PLAN.md`「跨切片一致性」。
- 出題引擎：純前端（`src/services/exam.ts`，S3 建立），斷網可考、恢復網路再同步成績。

## 專案必備文件(你要負責維護)

| 檔案 | 作用 | 誰寫 | 何時更新 |
|---|---|---|---|
| `PROGRESS.md` | 一頁進度儀表板：現在做到哪、下一步、待決事項 | 你(AI) | **每個 session 結束前必更新** |
| `DECISIONS.md` | 變更與決策日誌：任何偏離原始需求的改動 | 你(AI) | 每次發生偏離當下 |
| `REQUIREMENTS.md` | 階段①的白話功能清單(原始需求基準線) | 你(AI)，負責人簽核 | 需求變更時(並在 DECISIONS.md 留痕) |
| `UI-UX-CHECKLIST.md` | UI/UX 檢核清單 + 你的自查結果 | 你(AI) | 階段②(UX)與交付前(UI)。**本專案暫由 `docs/uiux/` 承接此功能，尚未另建此檔** |
| `AGENTS.md` | 本規範 | 你(AI) | 規則或架構改變時 |

## 五階段關卡(Gated Workflow)v2

### ① 系統分析
- 交付 `REQUIREMENTS.md`：白話功能清單(每條「使用者能…」+「給定/當/則」驗收句)、「先不做」清單。一次給一個區塊。
- 建立 `PROGRESS.md` 初版。
- **Exit Gate：** 負責人確認清單且範圍凍結。

### ②a 系統設計 — 介面 + UX 早期檢核
- 交付**可點擊的靜態雛形**(假資料)，每畫面標出對應哪些功能。
- **UX 早期檢核(此時做，因為改流程最便宜)：** 對照 `UI-UX-CHECKLIST.md` 的 UX 區(以 Nielsen Norman 10 大可用性原則為底)，逐條自查並用白話寫結果：操作幾步能完成？流程有沒有繞？錯誤狀態想過了嗎？
- **Exit Gate：** 負責人點過雛形、UX 檢核通過，才開始寫後端。

### ③ 程式開發 — 垂直切片
- 拆成垂直切片，一次做一個，做完**停下來**給：白話「現在你能做什麼」+ 操作步驟 + 關鍵截圖。
- 每片能在負責人環境實際跑。每完成一片更新 `PROGRESS.md`。
- **Exit Gate：** 負責人親自操作該切片並確認，才做下一片。

### ④ 測試 — 含需求漂移管控
- 交付對照 `REQUIREMENTS.md` 的**驗收表**(已測/未測/失敗，用「給定/當/則」當案例，含邊界)。
- **若測試或修 bug 導致與原始需求不同 → 必須：(1) 在 `DECISIONS.md` 記錄(改了什麼/為什麼/影響哪條原始需求/負責人是否同意)；(2) 同步更新 `REQUIREMENTS.md`，讓基準線永遠是最新真相。**
- **Exit Gate：** 在乎的功能全部已測，且所有偏離都已記錄並經負責人確認。

### ⑤ 交付上線 — UI 交付前檢核 + 上線檢查
- **UI 交付前檢核(視覺收尾)：** 用瀏覽器**自動截圖**主要畫面(桌機+手機尺寸)，對照 `UI-UX-CHECKLIST.md` 的 UI 區逐條自查(對齊、間距、字級一致、RWD、空狀態、載入狀態)，把截圖與逐條結果交給負責人做最後人工確認。
- 交付**白話上線檢查表**(從使用者角度)+ 一句話「上線後能做什麼」+ 白話 rollback。
- **交付兩份成果文件(見下方「成果文件交付閘」)：** `USER_MANUAL.md`、`DEVELOPER_LOG.md`。**本專案這兩份已存在（2026-05-31 建立），視為已通過此關卡的一部分。**
- 更新 `PROGRESS.md` 標記專案狀態。
- **Exit Gate：** UI 檢核截圖經負責人確認、負責人從乾淨環境走過、檢查表全勾、**且成果文件交付閘已通過**。

## ★ 成果文件交付閘(階段⑤必檢，不做會被漏掉的那一關)

> 教訓：`USER_MANUAL.md` 與 `DEVELOPER_LOG.md` 常被寫成「應該做」卻沒人提醒，結果專案標了「完成」卻缺文件。
> 因此把它們設成**明確的閘**：任何要把專案標成「已交付 / 完成 / 進度 100%」的動作，**之前**必須先過這一關。

**觸發時機：** 你(規劃層或執行層)準備把 `PROGRESS.md` 標成「已交付 / 完成」或整體進度標到 100% 之前。

**必做檢查：**
1. 確認專案根有 `USER_MANUAL.md`(給不懂程式的使用者：怎麼用)與 `DEVELOPER_LOG.md`(給一兩個月後回來的自己：環境怎麼跑、主要模組、重要架構決定)。**本專案兩者皆已存在。**
2. **缺任一份 → 停下，提醒負責人：「交付前還欠這份文件，要現在補嗎？」** 不可靜默略過、不可在缺文件又無留痕的情況下宣告交付。
3. 若負責人決定**略過**(例：內部小工具、一次性腳本，不需完整手冊)→ 在 `DECISIONS.md` 記一筆(略過哪份、原因、負責人同意)，才可標交付。

## DECISIONS.md 記錄格式(每筆)
```
## [日期] 決策標題
- 狀態：已採納 / 已否決 / 待確認
- 背景：為什麼要改(測試發現什麼 / bug / 新限制)
- 改動：具體改了什麼
- 影響的原始需求：REQUIREMENTS.md 第 X 條(原本…→ 現在…)
- 負責人是否同意：是 / 否 / 待確認
```
狀態為「待確認」時，AI 不可逕自實作，必須先得到負責人同意。

## 溝通規則
- 對負責人不用技術名詞；必要時先給白話比喻。
- 報告永遠先講「使用者現在能做什麼」。
- 每次停下來明確問：「這一關通過了嗎？可以往下嗎？」
- 每個 session 開始時，先讀 `PROGRESS.md` 恢復脈絡；結束前必更新它。

## 規劃層開場 / 收尾 SOP(取代 hook，規劃層多半無 hook 機制)

規劃層不一定能掛 hook，靠下列固定動作維持紀律(已包成 skill `planner-sop`)：

**每次開工：** (1) 先讀 `PROGRESS.md` 恢復脈絡；(2) 確認在五階段哪一關、下一步；(3) 要進新階段先寫 `STAGE_N_PLAN.md`，要開切片先寫 `BRIEF`。

**每次收工：** (1) 需求/範圍變動 → 更新 `REQUIREMENTS.md` 並在 `DECISIONS.md` 留痕；(2) 規則/架構變動 → 更新本檔 `AGENTS.md`；(3) 明確問負責人：「這一關過了嗎？可以往下嗎？」

## 執行層自動化機制(目前：尚未鋪設 Codex hooks)
本方法論理想上由執行層的 hook 強制觸發文件更新，不靠 AI 自律：
- `SessionStart` → 自動讀 PROGRESS.md 注入模型脈絡。
- `PostToolUse(改檔工具)` → 執行層實際改動檔案後提醒記 DECISIONS.md。
- `Stop` → 收工前檢查 PROGRESS.md 今天是否更新，未更新則擋下要求補。

> **本專案目前沒有 `.codex/hooks.json`。** 在補上之前，執行層（Codex CLI）開工/收工請比照 `WORKFLOW.md` §5.3 的等價做法：每份 BRIEF 的開場與收尾指令裡明寫「先讀 PROGRESS.md / 收工更新 PROGRESS.md / 改動需求記 DECISIONS.md」。
