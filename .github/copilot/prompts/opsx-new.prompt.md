---
agent: agent
description: "OpenSpec：建立新 Change"
---
請使用 `openspec-new-change` skill 建立新的 Change。

步驟：
1. 在 `openspec/changes/` 下建立 change 目錄
2. 建立 proposal artifact
3. 確認 Change 名稱和範疇
4. 更新 `docs/runlog/` 記錄本次操作

完成後告訴我下一步該執行什麼（建議 `#opsx-ff` 快進生成所有 artifacts）。
