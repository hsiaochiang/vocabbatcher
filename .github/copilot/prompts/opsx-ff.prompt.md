---
agent: agent
description: "OpenSpec：快進生成所有 artifacts"
---
請使用 `openspec-ff-change` skill 快進完成目前進行中 Change 的所有 artifacts。

這會自動生成：proposal → spec → delta-spec → tasks 等所有必要文件。
完成後提示我進行 `#opsx-validate` 驗證。
