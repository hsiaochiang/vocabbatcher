# Runlog 2026-05-31

## 本日目標
執行功能補完清單（A-1 ~ C-2），讓 `vocab.cleaned.json` 含有中文釋義、詞性、PDF 頁碼與 IPA 音標。
建立 B-1 Web App（ExamVocabBatcher PWA），完成 M2 核心功能。

## 完成事項

### v1（15:35 提交）

#### A-1：新增 Markdown 解析器
- 新增 `src/pdf_parser/rules/top2025_md.py`：獨立的 `parse_md_file()` 函式
- 修改 `src/pdf_parser/__main__.py`：CLI 自動偵測 `.md` 輸入，走 Markdown pipeline
- 新增 `tests/test_top2025_md.py`：13 個測試覆蓋邊界情況
- 結果：1231 筆全部成功解析，zh_definition 覆蓋率 97%，pos 覆蓋率 96%

#### A-2：parse_confidence 公式修正
- 修改 `src/pdf_parser/qa.py`：optional field weight 從 0.2 調為 0.5
- word-only 信心分數從 0.50 降至 0.29（≤ 0.3 ✓）

#### A-3：解析失敗行追蹤
- 新增 `ParseResult` dataclass（parser.py）
- QAReport 新增 `rejected_lines` 欄位

#### B-3：load_rule() TypeError 防護
- 加 try/except TypeError 包裹 `attr()` 呼叫

#### B-4：cleaner.py trim 順序修正
- 在 `compute_confidence/compute_issues` 前先建立 normalized copy

#### C-1：output/ 加入 .gitignore

### v2（16:07 提交）

#### PDF 頁碼補充
- 從 PDF 提取 word→page 對應（pdfplumber，無 OCR）
- 修改 `0resource/top2025.md` 新增第四欄「頁碼」
- 更新 MD parser 讀取頁碼欄位
- 結果：1231 筆全部有正確頁碼（第 3–62 頁）

#### C-2：IPA 音標補充
- 新增 `src/pdf_parser/fetch_ipa.py`：批次 API 查詢工具
- 從 dictionaryapi.dev 取得 1231 筆 IPA（限流 0.5s/req，快取續傳）
- US 音標 93.7%（1154 筆），UK 音標 30.6%（377 筆）
- 快取存於 `output/ipa_cache.json`

#### C-3：--min-frequency（部分完成）
- CLI 端已正常運作
- App 端 UI 需等 B-1 完成

## 測試
- 53 tests passed（v1: 52, v2: +1 新頁碼測試）

## 最終資料品質
| 欄位 | 覆蓋率 |
|------|--------|
| word | 100% |
| pos | 95.9% |
| zh_definition | 97.0% |
| frequency | 100% |
| source_page | 100% |
| ipa_us | 93.7% |
| ipa_uk | 30.6% |

### v3（B-1 Web App）

#### ExamVocabBatcher PWA
- 使用 `npm create vite@latest` 建立 React + TypeScript 專案
- 安裝 Tailwind CSS v4、React Router v6、vite-plugin-pwa
- 實作 4 個畫面：HomePage、BatchBuilderPage、BatchHubPage、FlashCardPage
- 實作共用元件：Header、WordCard、SelectionCounter、Toast
- 實作 AppContext 狀態管理 + localStorage 持久化
- 實作 TTS 服務（Web Speech API）
- 實作翻牌卡 3D 翻面動畫 + 進度追蹤
- 篩選功能：頻率（高/中/低）、詞性、搜尋
- PWA 支援：Service Worker + manifest + 離線快取
- Build 成功：JS 250KB (gzip 79KB)，共 6 entries precache

## 產出檔案
- `VERIFICATION.md`（根目錄）：v1+v2 驗收報告
- `exam-vocab-batcher/VERIFICATION.md`：B-1 Web App 驗收報告
- `exam-vocab-batcher/`：完整 Web App 專案
