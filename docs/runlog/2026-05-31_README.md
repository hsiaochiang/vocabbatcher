# Runlog 2026-05-31

## 本日目標
執行功能補完清單（A-1 ~ C-1），讓 `vocab.cleaned.json` 含有中文釋義與詞性。

## 完成事項

### A-1：新增 Markdown 解析器
- 新增 `src/pdf_parser/rules/top2025_md.py`：獨立的 `parse_md_file()` 函式
- 修改 `src/pdf_parser/__main__.py`：CLI 自動偵測 `.md` 輸入，走 Markdown pipeline
- 新增 `tests/test_top2025_md.py`：13 個測試覆蓋邊界情況
- 結果：1231 筆全部成功解析，zh_definition 覆蓋率 97%，pos 覆蓋率 96%

### A-2：parse_confidence 公式修正
- 修改 `src/pdf_parser/qa.py`：optional field weight 從 0.2 調為 0.5
- word-only 信心分數從 0.50 降至 0.29（≤ 0.3 ✓）
- 更新 `tests/test_qa.py` 對應斷言

### A-3：解析失敗行追蹤
- 新增 `ParseResult` dataclass（parser.py）
- `parse_pages()` 回傳型別從 `list[VocabEntry]` 改為 `ParseResult`
- QAReport 新增 `rejected_lines` 欄位
- 更新 CLI、QA report、測試

### B-3：load_rule() TypeError 防護
- 加 try/except TypeError 包裹 `attr()` 呼叫

### B-4：cleaner.py trim 順序修正
- 在 `compute_confidence/compute_issues` 前先建立 normalized copy
- 確保空白字串被正確轉為 None

### C-1：output/ 加入 .gitignore
- 新增 `output/` 到 `.gitignore`

## 測試
- 52 tests passed（原 37 + 新增 15）

## 產出檔案
- `VERIFICATION.md`：驗收報告
- `output/vocab.cleaned.json`：1231 筆，zh_def 97%，pos 96%
- `output/vocab.qa_report.json`：品質報告
