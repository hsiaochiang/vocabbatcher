請讀 D:\program\vocabbatcher\AGENTS.md 了解本專案的協作規範，你是執行層。

接著讀 D:\program\vocabbatcher\BRIEF_S3b_登入異常修復.md，這是你這次要做的完整施工規格（開場指令、已知現象、待查方向、任務、邊界、驗收標準、收工指令都在裡面），照著做：

1. 開工先讀 PROGRESS.md 恢復脈絡。
2. 工作目錄是 exam-vocab-batcher/。先重現問題（不用真手機，模擬手機 UA 或遠端偵錯連 Android 皆可），找出 getRedirectResult() 實際回傳值、有沒有例外被吞掉，判斷根因（BRIEF 裡列了三個待查方向：PWA Service Worker 快取干擾、Firebase 主控台設定、IndexedDB/sessionStorage 持久化問題），不要只挑一個假設就動手改，要先確認根因。
3. 修好後，因為負責人不會操作瀏覽器開發者工具，暫時在畫面上加一小段可視化除錯文字讓他能回報畫面上實際發生什麼；確認修復後要把這段除錯文字移除或收斂成正式錯誤提示，不可留在正式畫面上。
4. npm run lint、npm run build 要過。
5. 若修法偏離本 BRIEF 或原本 S2 的 BRIEF_S2_雲端帳號.md → 記進 DECISIONS.md。
6. 收工前更新 PROGRESS.md：記錄根因與修法，「▶ 下一步」改成「等負責人重新驗收登入，過了回頭繼續 S3 驗收項 3~6」。

不要動 S3 考試引擎本身的程式碼（src/services/exam.ts、ExamSetupPage/ExamRunPage/ExamResultPage），也不要重新設計登入方式（除非你診斷後認定 signInWithRedirect 本身在此情境不可行，這種情況先停下回報，不要自行換方案）。

## 最後一步：把執行結果寫成報告檔

完成以上所有工作後，在專案根目錄新建（或覆寫）`D:\program\vocabbatcher\REPORT_S3b_登入異常修復.md`，內容依這個格式寫：

```markdown
# REPORT_S3b_登入異常修復 — 執行結果

- 執行時間：<填你的執行時間>
- 狀態：完成 / 部分完成（卡在哪裡）/ 失敗

## 根因
<你診斷出的實際根因，用白話講清楚為什麼手機登入後畫面不會出現頭像>

## 改了什麼
<具體改了哪些檔案、加了什麼邏輯、為什麼這樣改就能解決>

## 是否偏離 BRIEF 或原始需求
<有的話，簡述已記錄在 DECISIONS.md 的哪一條；沒有就寫「無」>

## npm run lint / npm run build 結果
<過 / 不過，不過的話貼關鍵錯誤訊息>

## 負責人驗收步驟（白話）
<照 BRIEF_S3b 的「驗收（負責人操作）」章節，寫成負責人看得懂、能照做的步驟；
如果畫面上暫時留了除錯文字（第 3 點要求後來要移除），要特別提醒負責人「這是暫時的除錯文字，驗收完會拿掉」還是「已經拿掉了」。>

## 遇到的問題 / 卡住的地方（若有）
<如果沒辦法完全解決，寫清楚卡在哪、需要規劃層或負責人做什麼決定>
```

這份報告檔是給規劃層（Claude）看的，不是給負責人看的，所以可以直接寫技術細節，不用刻意白話（除了「負責人驗收步驟」那一段要白話，因為會轉給負責人）。寫完這份報告代表你這次工作結束，不用再額外輸出總結。
