# 功能補完清單

> 更新日期：2026-05-31（依 VERIFICATION.md v2 同步）
> 對象：自用，目標「先把功能補完整，UI/UX 美化之後再說」

---

## 資料層現況（已完成，可交付）

| 欄位 | 覆蓋率 | 來源 |
|------|--------|------|
| word | 100%（1231/1231）| top2025.md |
| pos | 95.9%（1181/1231）| top2025.md |
| zh_definition | 97.0%（1194/1231）| top2025.md |
| frequency | 100%（1231/1231）| top2025.md 章節標題 |
| source_page | 100%（1231/1231）| top2025.pdf 頁碼 |
| ipa_us | 93.7%（1154/1231）| dictionaryapi.dev |
| ipa_uk | 30.6%（377/1231）| dictionaryapi.dev |

**53 個自動測試全部通過。CLI pipeline 正常運作。**

---

## (A) 壞掉或缺了就沒辦法用的 — 最優先

> 資料層全部已修完，目前沒有 A 類阻塞項目。

| 項目 | 狀態 | 說明 |
|------|------|------|
| A-1 Markdown parser rule | ✅ 完成 | `src/pdf_parser/rules/top2025_md.py`，1231 筆含中文 |
| A-2 parse_confidence 公式 | ✅ 完成 | word-only 降至 0.29（≤ 0.3） |
| A-3 解析失敗行追蹤 | ✅ 完成 | QAReport 新增 `rejected_lines` |

---

## (B) 規劃好但還沒做或只做一半的功能

### B-1｜React Native App — M2 核心功能（完全不存在）

**狀態：** 未開始。目前無任何 .tsx / .ts / package.json。

**包含以下全部未實作：**

| 子項 | 說明 |
|------|------|
| B-1-1 Expo 專案初始化 | 建立 React Native + Expo 專案骨架，引入 vocab.cleaned.json |
| B-1-2 單字清單畫面 | 載入 JSON、顯示 1231 筆、支援搜尋 / 篩選（頁碼、頻率） |
| B-1-3 批次建立（勾選 ≤25 字） | 勾選計數器即時更新，25 滿額禁選 + 提示 |
| B-1-4 TTS 批次播放 | expo-speech：英文 → 間隔 → 中文 → 下一字，3/5/10 秒間隔可選 |
| B-1-5 播放進度顯示 | 播放中高亮目前單字 + 進度條 |
| B-1-6 翻牌卡學習 | 正面英文，背面中文 + 詞性（+ 音標選加） |
| B-1-7 效能與離線 | 首屏 < 2 秒（中階 Android）、斷網可用 |

**前置條件：** 無（資料層已就緒）
**難易度：** 高（需從頭建 Expo 專案）
**實際影響：** 這是整個 App 的主體。沒有它，使用者完全無法體驗任何核心功能。

---

### B-2｜React Native App — M3 練習功能（完全不存在）

**狀態：** 未開始。依賴 B-1 完成。

**包含以下全部未實作：**

| 子項 | 說明 |
|------|------|
| B-2-1 四題型練習 | 中→英、英→中、拼字填空、聽音選字（題庫限本批次） |
| B-2-2 即時判分 + 結果頁 | 每題作答後即時回饋，結束顯示分數 |
| B-2-3 錯題重練 | 結果頁可直接重練錯誤題目 |
| B-2-4 批次歷史列表 | 可重新進入歷史批次 |
| B-2-5 批次統計 | 正確率、錯題分布 |
| B-2-6 中斷自動儲存 | 首頁顯示「繼續上次」 |
| B-2-7 AsyncStorage 持久化 | 批次紀錄跨 session 保存 |

**前置條件：** B-1 完成
**難易度：** 高（練習邏輯 + 統計 + 持久化）
**實際影響：** 沒有這部分，App 只是單字清單瀏覽器，沒有學習閉環。

---

### B-3｜`--min-frequency` App 端 UI（部分完成）

**狀態：** ⚠️ 部分完成。CLI 端 `--min-frequency` 正常運作；App 篩選 UI 需等 B-1 完成後設計。

**前置條件：** B-1-2（單字清單畫面）完成時一起做
**難易度：** 低（B-1 完成後順手加）
**實際影響：** App 的頻率篩選體驗，屬加分功能，MVP 沒有也可用。

---

## (C) 可有可無、加分用的

### C-1｜已完成項目

| 項目 | 狀態 |
|------|------|
| output/ 加入 .gitignore | ✅ 完成 |
| PDF 頁碼補充到 top2025.md | ✅ 完成 |
| IPA 音標（ipa_us 93.7%、ipa_uk 30.6%） | ✅ 完成 |

---

### C-2｜ipa_uk 覆蓋率偏低

**狀態：** 現況 30.6%（377/1231）。
**說明：** dictionaryapi.dev 提供的英式音標本就比美式稀少。App 以 ipa_us 為主即可，ipa_uk 缺漏不影響核心功能。
**難易度：** 低至中（換用支援 UK IPA 的 API，如 Wiktionary API）
**實際影響：** 對 MVP 無影響；翻牌卡顯示美式音標已足夠。

### C-3｜M4 品質收斂（Release Quality）

**狀態：** 未開始，等 B-1 + B-2 完成後才有意義處理。

| 子項 | 說明 |
|------|------|
| 邊界情況處理 | 0 字批次阻止、重複批次提示、空白搜尋結果 |
| 效能優化 | FlatList 虛擬化，500+ 字滾動不卡頓 |
| Touch target ≥ 44dp | 全部可點選元素 |
| Smoke test | 全流程自動化冒煙測試 |
| README 使用說明 | 補充 App 安裝 / 執行說明 |

---

## 下一步建議

```
現在：
  B-1  建 Expo 專案 + M2 核心功能（批次選字、TTS 播放、翻牌卡）

完成 B-1 後：
  B-2  M3 練習功能（四題型 + 統計 + 歷史）
  B-3  --min-frequency App 端 UI（與 B-1-2 一起做）

最後：
  C-2  補 ipa_uk → 收斂到可接受覆蓋率
  C-3  M4 品質打磨 → Google Play 內部測試軌
```

> 資料層（vocab.cleaned.json）已就緒，B-1 可直接開始，不需要再等任何前置工作。
